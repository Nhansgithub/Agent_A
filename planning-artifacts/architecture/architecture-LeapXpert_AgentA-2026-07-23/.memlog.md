---
scope: PRD-to-UserDoc automation agent flow: multi-tenant single service that detects finalized PRDs in Confluence and drives an automated draft/review/publish pipeline for end-user help docs, gated by humans in Jira. Demo scope.
purpose: build-substrate
altitude: feature
updated: 2026-07-24T07:48
---

- (direction) Source of truth: PRD v0.3 (prd.md) + its memlog. Greenfield (no existing code to ratify); no project-context.md. Altitude=feature (this system decomposes into epics next). Purpose=build-substrate + concise human-facing solution-design companion.
- (constraint) [ADOPTED PRD 14 and 9] Paradigm: event-driven pipeline of small, independently webhook-triggered, mostly-stateless flow steps coordinated by an explicit shared state store (pipes-and-filters over an event bus of Atlassian webhooks). Binds: all flow logic. Prevents: units inventing implicit or in-memory cross-step state, or leaning on a framework to own the state contract.
- (constraint) [ADOPTED NFR-03/PRD 9-10] All per-PRD state lives in an explicit SQLite state store, reached ONLY through a repository layer; no logic touches raw SQL. Binds: every read/write of the per-PRD state record (section 10). Prevents: two modules diverging on schema/SQL, and blocks the SQLite->Postgres swap being more than a single-module change.
- (constraint) [ADOPTED PRD 1/14/NFR-06] Multi-tenant single service: one deployment routes every inbound webhook event to exactly one tenant via the config registry BEFORE any work happens (route-before-work). Binds: webhook ingestion + all downstream steps. Prevents: work executing without a resolved tenant, or tenant leakage across projects.
- (constraint) [ADOPTED NFR-06/EH-05] Serial queue: demo processes exactly one PRD at a time; state store tracks queued vs in-progress. Design must not preclude later parallelism (per-PRD row is the natural unit of isolation). Binds: the orchestrator/queue. Prevents: concurrent mutation of shared Atlassian/state resources in the demo, and a design that hard-codes single-flow assumptions.
- (constraint) [ADOPTED NFR-10/PRD 9] Orchestration runtime = LangGraph MIT-licensed core library ONLY, behind a self-built FastAPI wrapper. NO dependency on the Elastic-licensed langgraph-api / langgraph server product. Binds: orchestrator + deps manifest. Prevents: pulling in the licensed server product (license cost + a running server the 1GB box cannot afford).
- (constraint) [ADOPTED PRD 9/NFR-03] LangGraph models the Flow as a graph and owns checkpointing, but its checkpointer persists THROUGH the repository layer to the same SQLite file -- not a separate DB server and not a parallel state store. Binds: orchestrator checkpoint writes. Prevents: two competing state stores (LangGraph checkpoint DB vs the per-PRD state record) drifting out of sync.
- (constraint) [ADOPTED PRD 6.3/9] Role-agents (Classifier, Ticket manager, Author, Feedback interpreter, Publisher, Error handler) = per-role system prompt + SKILL.md over a shared agent runtime, wired as graph nodes; NOT separate services. All LLM calls go through the Anthropic Python SDK (Claude API). Binds: agent node implementations. Prevents: divergent per-role runtimes/SDKs and duplicated LLM-call plumbing.
- (constraint) [ADOPTED PRD 11/15.4] Integration = direct Atlassian Cloud REST (Jira Cloud REST v3 + Confluence Cloud REST) via dedicated adapter modules; API-token auth supplied by env credential refs (never inline secrets). No MCP. Binds: all Jira/Confluence access. Prevents: scattered ad-hoc HTTP calls, inline secrets, and a hidden dependency on an MCP layer the deploy target does not run.
- (constraint) [ADOPTED FR-01/15.4] Webhook ingestion over a single public HTTPS endpoint (Confluence page-created/updated; Jira comment-created + issue-transition), every request validated by shared-secret/signature check before processing. Binds: the webhook layer. Prevents: spoofed events triggering real Jira/Confluence writes, and unvalidated input entering the flow.
- (constraint) [ADOPTED NFR-01/PRD 15] Observability: LangSmith traces 100% of LLM calls (latency/tokens/cost) incl. review_round + per-round cost as the redraft-loop guardrail. Data-governance: demo traces non-confidential test PRDs only (redaction/retention deferred to prod). Binds: every agent node. Prevents: untraced LLM spend and an unobservable runaway redraft loop.
- (constraint) [ADOPTED PRD 15/NFR-07/NFR-11] Deploy = single Docker image (slim Python base) built OFF the Droplet (CI/registry) and PULLED -- never built on the 1GB box (OOM). Runtime: single Uvicorn worker; FastAPI bound to localhost behind a reverse proxy (Caddy default, nginx acceptable) terminating TLS; firewall opens only 443+22; add a 1-2GB swap file. Host: 1GB RAM / 1 vCPU / 25GB DigitalOcean Droplet, Ubuntu LTS. Binds: packaging + host topology. Prevents: on-box builds OOMing, multi-worker RAM blowout, and exposing FastAPI directly.
- (constraint) [ADOPTED NFR-11/PRD 15.2] Memory envelope is a first-class invariant: at most one PRD resident in memory at a time; lean deps only; no co-located DB server (SQLite is in-process); large PRD payloads are the main RAM consumer so the serial queue is also a memory-safety measure. 1GB is reversible (resize up if the section 12 run OOMs) -- do not hard-code a 1GB-only assumption. Binds: runtime + deps choices. Prevents: memory growth that breaks the stable-on-1GB requirement.
- (version) Python 3.12 (slim base image python:3.12-slim). Web-verified fit: FastAPI/uvicorn/langsmith require >=3.10; anthropic supports 3.9-3.14; langgraph 1.x fine. 3.12 chosen as conservative widely-supported slim base for the 1GB box.
- (version) FastAPI 0.136.x (latest 0.136.3, May 2026; newer on PyPI Jul 16 2026). Web-verified on PyPI. Self-built wrapper -- no fastapi[standard] extras needed beyond what is used.
- (version) Uvicorn 0.51.0. Web-verified PyPI. Single worker only (NFR-11 memory).
- (version) LangGraph (MIT core library 'langgraph') 1.2.9. WEB-VERIFIED CRITICAL: PyPI License Expression = MIT (SPDX). The langgraph-api server component is Elastic License 2.0 and is NOT used. Documented OSS-compliant path = use langgraph lib + skip the CLI/langgraph dev|build + build own FastAPI serving layer -- exactly the PRD NFR-10 choice.
- (version) langgraph-checkpoint-sqlite 3.1.0 (MIT per PyPI). Provides SqliteSaver + AsyncSqliteSaver (aiosqlite). Part of the langgraph MIT monorepo (libs/checkpoint-sqlite). Hardening: set LANGGRAPH_STRICT_MSGPACK=true (or explicit allowed_msgpack_modules) to restrict checkpoint deserialization.
- (version) Anthropic Python SDK 'anthropic' 0.117.0 (Jul 16 2026, MIT, Python 3.9-3.14). Web-verified PyPI. Sync+async, streaming. All Claude API calls go through this.
- (version) LangSmith SDK 'langsmith' 0.10.9 (Jul 20 2026, Python >=3.10). Web-verified PyPI. Has claude-agent-sdk + otel extras.
- (version) Caddy 2.11.4 (stable, Jun 3 2026). Web-verified. Reverse proxy + auto Let's Encrypt TLS. This release fixed CVE-2026-27585/6/7/8 (matcher bypass / TLS client-auth fail-open) -- pin >=2.11.4.
- (version) markdownify 1.2.3 (Jun 30 2026). Web-verified PyPI. Confluence storage-format (XHTML) -> Markdown; MarkdownConverter subclass overrides handle Atlassian ac:/ri: macro+resource tags. Lean single-purpose dep (fits 1GB).
- (version) Jira Cloud platform REST API v3 (base /rest/api/3/). Web-verified endpoints: GET issue/{key}/transitions returns ONLY currently-legal transitions from current status; POST issue/{key}/transitions performs transition; comment bodies require Atlassian Document Format (ADF), not plain string. Status done-ness detected via statusCategory.key == 'done' (tenant-agnostic, not literal name).
- (version) Confluence Cloud REST API: v2 (base /wiki/api/v2/) is current -- pages carry version.number; folders are first-class (GET /wiki/api/v2/folders/{id}, /children, /spaces/{id}/folders). BUT placing a page under a folder via v2 parentId 500s; use v1 move endpoint PUT /wiki/rest/api/content/{id}/move/append/{folderId}. Content restrictions are Cloud-only v1 endpoints /wiki/rest/api/content/{id}/restriction/*.
- (version) SQLite via Python stdlib sqlite3 (in-process, no server) -- the state store AND the LangGraph checkpoint backend, single file on the Droplet disk. No separate DB server co-located (NFR-11).
- (assumption) langgraph-checkpoint-sqlite 3.1.0 sdist is reported (GitHub discussion 5210) to omit the LICENSE file though PyPI metadata declares MIT and it lives in the MIT-licensed langgraph monorepo. Intent is MIT; flag for a quick legal sign-off before prod. Does not block demo.
- (assumption) Confluence webhook registration mechanism to confirm at build: with API-token (non-Connect) auth, page-created/updated + Jira comment-created/issue-updated webhooks are registered via product admin webhooks or Automation rules pointing at the public HTTPS endpoint with the shared secret. Webhook delivery is best-effort (not guaranteed) -- reinforces idempotency; a polling fallback is out of demo scope (PRD 13 Q3).
- (assumption) Confluence page-created webhook payload: creator resolved from the event's creator/authorId or a follow-up GET page (history.createdBy.accountId or version.authorId). PRD 13 Q4 flags verifying the creator field is present. Same-org Atlassian accountId is shared Jira<->Confluence (web-verified), so no mapping table is needed in the demo.
- (assumption) Jira v3 requires ADF for comment/description bodies; the Ticket-manager + Feedback-interpreter adapters build ADF documents (not plain strings) for every comment they post. Build a small ADF helper.
- (decision) AD boundary+dependency: seven layers with inward-only dependency -- webhook layer -> router -> orchestrator/graph -> role-agent nodes -> integration adapters + repository -> SQLite. Rule: ONLY adapters touch Atlassian and ONLY the repository touches SQLite; role-agent nodes and the orchestrator receive adapters/repository by injection and never open a socket or run SQL themselves. Binds: all modules. Prevents: an agent node reaching Atlassian/SQLite directly, so retry/auth/dedupe/version rules cannot be bypassed.
- (decision) AD state-mutation: the per-PRD state record (section 10) is the single shared datum and the repository is its sole owner. Rule: all reads/writes go through repository methods; stage is an explicit enum advanced ONLY by the orchestrator at a stage boundary (never inferred from Jira/Confluence, never written by a role-agent -- agents return results, the orchestrator persists them); business stage is authoritative over any Atlassian status. Binds: state record + stage. Prevents: two writers of one entity and stage inferred inconsistently from external systems.
- (decision) AD config-isolation (NFR-05): the config registry is the ONLY home for project-specific literals (jira project keys, confluence space/folder ids, pm/head/admin account ids, credential refs, md_export_dir). Rule: no such literal appears in code, prompts, SKILL.md, or the reserved-label constant; adapters+agents receive a resolved tenant-config object by injection; the tree is grep-clean of literals. Binds: all modules + prompts. Prevents: a hardcoded id in one unit silently breaking multi-tenant routing/modifiability.
- (decision) AD idempotency+dedupe (resolves 13.1 dedupe-key): idempotency key = composite {tenant_id}:{event_type}:{entity_id}:{version_marker}. Confluence page events -> entity_id=page id, version_marker=page version.number (monotonic int, bumps on every edit incl. rename/move). Jira comment-created -> entity_id=comment id (unique). Jira issue-updated/transition -> entity_id=issue key, version_marker=changelog(history) id of that transition. Rule: keys live in a repository-owned processed_events table (per-tenant, NOT nested in a PRD row, because page-created arrives before any PRD row exists); the webhook layer checks+records the key at ingestion BEFORE routing/work, in one transaction. Binds: every webhook handler. Prevents: duplicate deliveries double-processing while a genuine rename/page-updated (new version.number) re-enters (NFR-04, EH-04); and two units disagreeing on the key (page-id-only vs version) which would either loop or drop renames.
- (decision) AD detection-exclusion (resolves 13.1 guard): a page enters FR-01 detection ONLY IF (a) its location resolves to the tenant confluence_source_folder_id AND (b) it does NOT carry the reserved system label 'agent-generated' AND (c) its creator accountId is not the agent's own account. Primary guard is structural -- the published folder is a different (adjacent) folder id so published pages are never in the watched set; the label + self-author checks are defense-in-depth. The Publisher SHALL stamp the 'agent-generated' label on every page it creates/publishes. The reserved label is a fixed system constant (same for all tenants) so it stays config-isolation-clean. Binds: detection + Publisher. Prevents: the agent's own output being re-ingested into an infinite draft loop (FR-15 -> FR-01).
- (decision) AD checkpoint-granularity (resolves 13.1 checkpoint + reconciles LangGraph): the resumable unit is the section-9 STAGE. The graph is built so each stage is a node (or small cluster) with a checkpoint boundary; the LangGraph checkpointer (SqliteSaver) persists through the repository to the SAME SQLite file, thread_id = prd_id (Confluence page id). Rule: at each stage boundary the orchestrator commits, in ONE repository transaction, both the state-record stage transition and the LangGraph checkpoint, and records last_good_checkpoint = that checkpoint cursor -- so the business stage and the graph cursor can never diverge. EH-02 resume re-enters the graph from last_good_checkpoint (the failed stage), never from the start. Binds: orchestrator + repository. Prevents: two competing state stores drifting, and inconsistent sub-stage-vs-whole-flow resume granularity.
- (decision) AD identity-mapping (resolves 13.1 Confluence->Jira): within one Atlassian org the accountId is shared across Jira and Confluence (web-verified), so the Uploading PM = the Confluence page-creator accountId resolved at runtime from the event and used DIRECTLY as the Jira assignee accountId for the FR-02a rename task -- no mapping table in the demo. Seam: an optional config identity_overrides map (confluence accountId -> jira accountId) plus an email-match fallback covers the cross-org edge (out of demo scope to auto-resolve). Binds: Ticket manager (rename-task assignment). Prevents: assigning the config Reviewer PM instead of the page author, or inventing a mapping service.
- (decision) AD transition-legality (resolves 13.1 Jira transitions, FR-04): to drive a ticket to Done the Ticket manager (1) reads current status; (2) if statusCategory.key=='done' -> SKIP (idempotent); (3) else GET /issue/{key}/transitions for the legal set from the current status; (4) if a transition whose target status has statusCategory 'done' exists -> perform it; (5) if none is directly available (mandatory intermediate states) -> raise EH-01 to the admin rather than guessing (auto multi-hop is out of demo scope; a config-declared preferred path is the seam). Done-ness is judged by statusCategory 'done', NOT a literal status name (tenant-agnostic). Rule: the agent AUTO-transitions ONLY the PRD-tracking ticket (FR-04); it MUST NEVER transition a human-gate ticket (Review/Publishing) -- those move by humans only. Binds: Ticket manager. Prevents: assuming a direct-to-Done path, double-transitioning an already-Done ticket, and an agent short-circuiting a human gate.
- (decision) AD folder-model (resolves 13.1 folder-vs-parent): Confluence folders are first-class and addressable by id (v2 folders API: GET /wiki/api/v2/folders/{id} + /children + /spaces/{id}/folders); config stores source/draft/published folder ids. Rule: placing a page INTO a folder uses the v1 move endpoint PUT /wiki/rest/api/content/{id}/move/append/{folderId} (v2 create/update parentId 500s on folder parents); detection's location check compares the page's folder/ancestor id to confluence_source_folder_id. Binds: Confluence adapter + detection + Publisher. Prevents: adapters modeling folders inconsistently (some as parent pages, some as folder ids) and failed placements via the broken parentId path.
- (decision) AD adapter-contract: all Atlassian access flows through two adapter modules -- JiraAdapter and ConfluenceAdapter -- exposing DOMAIN methods (e.g. search_issue, get_transitions, transition_issue, add_comment(adf), create_issue, get_page, create_page, move_page, set_edit_restriction, get/add_label, storage_to_markdown). Adapters own base-url+version selection (v2 default, v1 for move/restrictions), env-ref token auth, retry-with-backoff (NFR-08, ~3 retries), ADF construction for all comment/description bodies, and error normalization into a single AgentError type the Error handler consumes. Binds: every Atlassian call site. Prevents: scattered raw HTTP, inconsistent retry/auth, mixed API versions, and ADF handled in some call sites but not others.
- (decision) AD human-gate model (FR-12/FR-14): the SOLE approval signal at each gate is a human transitioning the gate ticket into a done-category status -- Review ticket Done = PASS, Publishing ticket Done = approve-to-publish -- detected via the Jira issue-updated webhook. Rule: the agent DETECTS gate Done and NEVER transitions a gate ticket itself; on no human action the run PARKS at awaiting_review / awaiting_publish_approval indefinitely (no timeout, no auto-escalation); feedback added after Done is ignored; non-Done terminal transitions + reassignment are out of demo scope (park). Binds: orchestrator + Feedback interpreter + Publisher. Prevents: an agent short-circuiting a human gate, or two units disagreeing on what 'approved' means.
- (decision) AD loop-safety (FR-08/FR-10/FR-11, NFR-09): the clarification loop (FR-08, only its 4 enumerated triggers) and the plain-language structure-confirmation loop (FR-10) BLOCK on a human reply and must never fabricate the answer or auto-advance (EH-08); outside the 4 triggers the agent proceeds with a stated assumption. The redraft loop (FR-07->FR-11) is uncapped but CANNOT self-spin -- each round requires a fresh human PM feedback comment; review_round increments per applied round and is surfaced with per-round token cost in LangSmith as the guardrail (no hard cap). Binds: Feedback interpreter + Author + orchestrator. Prevents: an autonomous spin/self-approval and inconsistent when-to-ask judgment.
- (decision) AD acceptance-oracles (FR-05/FR-03): the human PM PASS (FR-12) is the SOLE content-quality acceptance of the UserDoc; the Author runs exactly ONE draft->critique->revise self-critique pass as a drafting aid that never signals done-ness. The Classifier's objective bar is 0 false-positives / 0 false-negatives on the shipped labeled fixture set (FR-03 rubric). Binds: Author + Classifier epics. Prevents: treating self-critique as an approval gate (bypassing the human) and shipping a Classifier with no measurable accuracy bar.
- (decision) AD publish-transaction (FR-15): on Publishing-ticket Done the Publisher performs, IN ORDER and each idempotent/re-runnable from the checkpoint: (1) apply Confluence edit restriction (update-restriction) that MUST include the agent account (and space admins) or the API 400s/locks out -- this restricts who may edit, it is NOT a content freeze/version pin; (2) move the page via v1 move/append into confluence_published_folder_id; (3) export storage->markdown (markdownify) to the tenant md_export_dir on server disk; (4) mark state complete. Binds: Publisher. Prevents: partial-publish inconsistency, accidental self-lockout, and re-ingestion (published folder is outside the watched set, AD detection-exclusion).
- (decision) AD error+resume contract (EH-01/EH-02): on any error after NFR-08 retries the orchestrator sets state stage=error preserving last_good_checkpoint + pending_gate, and the Error handler posts EXACTLY ONE structured comment on the relevant ticket -- plain-language error + suggested fix + @admin (config) + the literal resume instruction ('Reply @agent resume or fixed on this comment and I will retry from where I stopped') + a correlation id logged to LangSmith. Resume: an admin comment webhook on that ticket containing '@agent resume'/'fixed' re-runs from last_good_checkpoint (the failed STAGE per AD checkpoint-granularity), never the whole flow; the resume comment is dedupe-guarded so a duplicate delivery cannot double-resume. Binds: Error handler + orchestrator. Prevents: divergent error surfaces, whole-flow restarts, and double-resume.
- (question) Instance-dependent (needs the real Atlassian tenant, cannot verify headless): confirm the Confluence page-created/updated webhook + Jira comment-created/issue-updated webhook are enabled and can reach the Droplet HTTPS endpoint (PRD 13 Q3), and that the Confluence page-created payload carries the creator accountId (else a follow-up GET page is needed) (PRD 13 Q4). Affects wiring detail only, not the boundary architecture.
- (question) Confirm the demo Jira project's workflow has a legal path from the tracking-ticket start status to a done-category status without a mandatory screen the agent cannot fill; AD transition-legality escalates to admin if not, but the demo should verify one exists up front (PRD 13 Q1).
- (assumption) HTTP client for the two Atlassian adapters = httpx (async, already transitive via anthropic + langsmith); pin the exact version at build. Kept out of the Stack table to avoid asserting an unverified version.
- (assumption) Reserved detection-exclusion label defaults to 'agent-generated'; if the org already uses that label, override via a single system constant. Not a per-tenant literal.
- (direction) Deferred (seams, not built now): SQLite->Postgres via the repository + a Postgres LangGraph checkpointer; RAG over published docs; SSG build/deploy downstream of the exported .md; true parallel multi-tenant execution; multi-approver publishing; fixed doc template; auto multi-hop Jira transition path; cross-org identity auto-resolution; LangSmith redaction/retention for confidential content; webhook polling fallback.
- (event) Spine distilled from memlog: 21 ADs (7 RESOLVES 13.1, 14 ADOPTED), paradigm=event-driven pipeline over explicit state store + LangGraph state-graph, 4 mermaid diagrams (dependency, container, happy-path sequence, deployment), Stack pinned to web-verified versions, Capability->Architecture map, Deferred section.
- (event) lint_spine.py: 0 high / 0 medium; 4 low template-token hits, all canonical Atlassian REST path params ({key},{id}) -- accepted as correct notation, not placeholders. Dedupe-key schema switched to angle-bracket notation to remove 8 false low hits.
- (event) Reviewer Gate ran (headless, sequential-fallback: no subagent dispatch tool available). Lenses: rubric walker + finalize_reviewer#1 web/version reality-check + finalize_reviewer#2 adversarial two-units-diverge. Reviews written to reviews/.
- (event) Gate findings: version reality-check PASS (all Stack rows web-verified; note langgraph-checkpoint-sqlite LICENSE-file caveat). Adversarial found 3 high + 1 medium holes: (A) dedupe two-owners section10 vs processed_events; (B) non-idempotent creates on stage resume; (C) dedupe record-before-work can drop an event on crash; (D) agent-account single source.
- (decision) Applied gate fixes to spine: AD-9 now names processed_events the single dedupe store (section10 dedupe_keys is a read-only view) with a UNIQUE constraint and records the key at flow admission not on receipt (fixes A+C); AD-11 adds resume-idempotency -- creates guarded by recorded ids, never double-create (fixes B); AD-10 gives the agent account one source (adapter get_current_user per tenant) referenced by AD-18 (fixes D); AD-8 wording aligned; Capability map gains EH-04/05/06/07/08/09 rows.
- (event) Post-fix lint: 0 high / 0 medium; 4 low = canonical Atlassian REST path params ({key},{id}) only, accepted.
- (event) spine finalized
- (event) Roundtable stress test (4 personas: Amelia/Winston/Dana/John) run against the finalized spine. Update intent opened: 2 user decisions (Q1,Q2) + 6 hardening refinements.
- (decision by user) Q1 [Nhan]: KEEP LangGraph but FIX AD-11. Amelia showed LangGraph SqliteSaver owns its own sqlite3 connection and commits inside .put(), so an atomic cross-store transaction with the repository state write is UNBUILDABLE -- the old AD-11 'commit both in one transaction' claim was false. Winston: collapse to ONE store. Resolution: the repository-owned SQLite state record is the SINGLE authoritative durable store; LangGraph stays but as in-invocation control flow ONLY.
- (decision by user) Q2 [Nhan]: FULL HARDENING over demo-trim. John (PM) proposed trimming AD-9 multi-shape dedupe / AD-12 cross-org identity fallback / AD-13 multi-hop transition handling to demo-only; Nhan chose to keep all three fully specced and IN build scope. Deferred keeps only genuine post-demo items.
- (decision) AD-11 REWRITTEN: the two-store 'one transaction' claim AND the two-store reconciliation are DELETED. New Rule: state record = single authoritative durable store; resumable unit = the section-9 stage; LangGraph = in-invocation control flow only (on each webhook: load the state record, (re)enter the graph at stage/last_good_checkpoint, run the stages that can advance without a new external event, persist the new stage + recorded ids via the repository, then stop); LangGraph's own checkpointer is NOT a cross-webhook durable store (nothing to reconcile). Resume correctness = idempotent-create replay: every externally-visible create (draft page, Review/tracking/Publishing ticket) guarded by the id recorded in the state record -- reuse if present, create only if absent, persist id in the same repository transaction that advances the stage; re-running a failed stage converges, never double-creates. Binds: orchestrator+repository. Prevents: two durable stores diverging (there is now only one) and double-create on resume.
- (decision) AD-2 amended: the per-PRD state record is the SINGLE durable truth -- the repository-owned SQLite file is the only authoritative durable store for run state. Atlassian holds content but is NOT a run-state reconstruction source (see AD-23). Binds: state record+stage. Prevents: a second competing durable store.
- (decision) AD-6 amended: clarify LangGraph = in-invocation ORCHESTRATION/control-flow model, NOT a durable state store; MIT-core-only licensing rule kept (langgraph core lib only, no Elastic-licensed langgraph-api server).
- (version) DROP langgraph-checkpoint-sqlite (SqliteSaver cross-webhook checkpointer is no longer the durable store, AD-11). Use in-invocation InMemorySaver/MemorySaver from langgraph.checkpoint.memory -- part of langgraph-checkpoint base (4.1.1, MIT, web-verified PyPI 2026-07-24; provides InMemorySaver since v1.0), a required dep already pulled by langgraph core 1.2.9. Checkpointer is ephemeral per-invocation; durability = the repository state record only. This DISSOLVES the prior langgraph-checkpoint-sqlite LICENSE-file caveat (package removed).
- (version) ADD litestream 0.5.15 (latest, Jul 21 2026, Apache-2.0, Go binary; web-verified GitHub releases) for AD-23 state DR. Pin floor >=0.5.4 (auto-disables aws-chunked encoding -> fixes DO Spaces upload failures). Streams the SQLite WAL to DO Spaces (S3-compatible). Small-RAM sidecar/host process, respects AD-21 envelope.
- (decision) AD-22 NEW -- Reconciliation & liveness sweep. Lightweight scheduled reconciler: PRIMARY = system cron on the Droplet -> authenticated localhost admin endpoint (zero new Python dep, respects AD-21 1GB/single-worker); SEAM = in-process APScheduler. Periodically (a) find runs in awaiting_review/awaiting_publish_approval/error whose updated_at is older than a threshold and ALERT via the EH-01 admin surface + a log/LangSmith signal (stuck or silently-dropped-webhook runs become visible); (b) re-poll the two gate tickets via the Jira adapter -- if statusCategory==done, feed as an INPUT (same effect as the missed issue-updated webhook). Reconcile w/ AD-2/AD-15: a gate-Done seen by webhook OR reconcile-poll is an INPUT only; the orchestrator still owns the stage; the agent still never transitions a gate ticket; indefinite-park is unchanged (recoverability+observability, NOT a timeout). NO double-advance -- three defenses: reconcile findings are admitted through the SAME webhook/admission + serial-queue path (AD-5; never a concurrent graph entry); the reconcile-poll derives the SAME AD-9 dedupe key (issue key + changelog history id of the to-done transition) so a racing webhook+poll collide on the UNIQUE constraint and one is dropped; and the orchestrator's stage advance is idempotent -- a gate-Done input arriving when already past that gate is a no-op. Un-defers the minimal polling fallback for the two gates. Rationale: Dana+Winston (a dropped best-effort webhook strands a correct human; parked runs emit zero LangSmith signal). Binds: reconciler+orchestrator+Jira adapter. Prevents: a silently-dropped gate webhook stranding a run, and unobservable parked runs.
- (decision) AD-17 refined -- held-out fixtures + eval as distribution. Split fixtures/classifier/{dev,holdout}/; the 0-FP/0-FN bar applies to HOLDOUT ONLY (dev set tunes the prompt) -- no train-on-test. Classifier acceptance = eval run x3 emitting a confusion matrix + a flake budget, classifier model id PINNED in config (AD-4); not a single boolean. Both fixture sets are build deliverables. (Amelia.)
- (decision) AD-16 refined -- typed FeedbackDecision. The Feedback interpreter node returns a typed FeedbackDecision{route, trigger, assumption} in app/domain/; the orchestrator's routing is DETERMINISTIC and unit-tested on hand-built decision objects; only the LLM producing the decision is eval-tested (held-out). Keeps stage-routing out of untestable prose. (Amelia.)
- (decision) AD-18 refined -- per-side-effect publish idempotency. Each of the 4 ordered publish side-effects (edit-restriction, move, export .md, mark complete) gets an individual idempotency marker / sub-checkpoint in the state record, so resuming the publishing stage skips completed side-effects and never re-applies (move = no-op if already placed; restriction re-apply must still include the agent account per AD-10). Per-call idempotency is a TESTED deliverable. (Winston.)
- (decision) AD-23 NEW -- state backup/DR. last_good_checkpoint, processed_events, and recorded ids are authoritative-ONLY-on-disk (AD-11/AD-2), so add a durability control: PRIMARY = litestream (>=0.5.4; 0.5.15 current) continuously streaming the SQLite WAL to DO Spaces (S3-compatible), a small-RAM sidecar/host process respecting AD-21; ALTERNATIVE = hourly sqlite3 .backup + /data tar to Spaces via cron. Full-hardening -> the backup is BUILT, not just noted. Corrects the 'state is reconstructable from Atlassian' claim (solution-design section 1): run state is NOT fully reconstructable -- losing the disk mid-run causes double-create/re-publish on webhook redelivery. Binds: deploy/ops + repository disk. Prevents: unrecoverable run-state loss and post-crash double-create/re-publish. (Dana.)
- (decision) AD-20 amended (proportionate; does not over-reach past the PRD deferral): keep the PRD-signed-off demo stance (non-confidential test PRDs only); ADD a content-gating config-flag seam (a config flag governs WHAT content is traced) + a one-line production note on the single-.env per-tenant token blast radius / rotation. Full redaction/retention stays deferred. (Dana.)
- (decision) Full-hardening (Q2) applied to AD-12 + AD-13: AD-12 -- the cross-org identity FALLBACK (config identity_overrides map + email-match) is now IN build scope (built), not a mere seam; only fully-automatic cross-org AUTO-resolution stays deferred. AD-13 -- the config-declared preferred multi-hop transition path is now IN build scope (traverse hop-by-hop, re-checking the legal transition set at each hop; escalate to admin only if the configured path is missing or a hop is blocked); only fully-automatic workflow-graph path-search stays deferred. AD-9 multi-shape dedupe was already fully specced -- unchanged, confirmed in scope.
- (direction) Deferred section updated: REMOVE 'webhook polling fallback' (un-deferred by AD-22 for the two gates) and the standalone 'auto multi-hop transition path' (handling now in scope via AD-13). Deferred now retains only genuine post-demo items: SQLite->Postgres, RAG over published docs, SSG build/deploy, true parallel multi-tenant execution, multi-approver publishing, cross-org identity AUTO-resolution, fully-automatic multi-hop path-search, fixed doc template, LangSmith redaction/retention for confidential content.
- (version) Verified langgraph core pins langgraph-checkpoint >=3.0.1,<5 (recent LangChain setup docs), so the base checkpoint 4.1.1 (providing InMemorySaver) is in-range for langgraph core -- compat confirmed. Stack row records the pin range.
- (decision) Gate pre-fix (adversarial self-pass on AD-11): recorded-id-only guard leaves a create-succeeded-then-crash-before-commit window that would double-create on replay (orphan artifact). HARDENED AD-11: 'absent' now triggers a find-or-create keyed on a deterministic marker (prd_id stamped as a Jira label/entity-property + a Confluence content-property) -- an orphaned create is ADOPTED, not duplicated. Companion 5.3 updated to match.
- (decision) Gate pre-fix (adversarial self-pass on AD-22): liveness alert would re-fire every sweep + reconciler must not write stage. HARDENED AD-22: alert recorded once per threshold crossing via a liveness_alerted_at marker; reconciler writes only non-stage markers through the repository and never advances stage (a found gate-Done is fed as an input, not written as a transition).
- (event) Spine re-distilled (r2): AD-1..AD-23 (+AD-22 reconciler, +AD-23 DR; AD-11 rewritten single-store+idempotent-replay; AD-2/6/12/13/16/17/18/20/21 refined). Diagrams updated (dependency + container labels single durable store; deployment +litestream/cron/Spaces). Stack: dropped langgraph-checkpoint-sqlite, added langgraph-checkpoint base 4.1.1 + litestream 0.5.15. Capability map +4 rows; Deferred trimmed (removed webhook-polling-fallback + standalone auto-multi-hop). Companion updated to match.
- (event) lint_spine.py (r2): 0 high / 0 medium; 4 low = canonical Atlassian REST path params ({key},{id}) only, accepted as correct notation. Effectively PASS.
- (event) Reviewer Gate ran (headless, sequential-fallback: no subagent dispatch tool). Lenses: rubric walker + finalize_reviewer#1 version reality-check + finalize_reviewer#2 adversarial two-units-diverge. Reviews written to reviews/*-r2.md.
- (event) Gate findings: rubric PASS (3 low build-details). Version reality-check PASS (langgraph-checkpoint 4.1.1 in core pin >=3.0.1,<5; litestream 0.5.15 floor >=0.5.4; -sqlite dropped so LICENSE nit retired; InMemorySaver 'not for prod durability' caveat satisfied since it is in-invocation only). Adversarial CONFIRMED both mandates -- no two-store divergence (single store by construction; ephemeral InMemorySaver rebuilt from the record) and no AD-22 double-advance (serial-queue admission + same dedupe-key collision + idempotent stage advance). Found+fixed pre-finalize: (B) create-succeeded-then-crash-before-commit double-create -> AD-11 find-or-create-by-deterministic-marker (adopt orphan); (D) reconciler stage-write -> AD-22 markers-only-through-repository, gate-Done fed as input; (E) liveness alert spam -> liveness_alerted_at once-per-threshold.
- (question) OPEN (build, low, AD-11 residual F): Confluence page-create + set-content-property is two calls, so the adoption marker is not atomic with the create; confirm the structural-search fallback (agent-author + draft-folder id + PRD linkage) reliably adopts an orphaned draft page created in the crash window, or set the marker via the create body if the API allows. Jira side is atomic (labels in the createIssue payload). Not a blocker.
- (event) spine finalized (r2)
